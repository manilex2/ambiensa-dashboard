# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on merge
'on':
  push:
    branches:
      - main
jobs:
  build_deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: npm i

      - uses: actions/checkout@v4
      - run:  cp src/environments/environment.ts src/environments/environment.prod.ts

      - uses: actions/checkout@v4
      - run: |
          sed "s/URL DEL SERVIDOR EXPRESS/${{ secrets.SERVERURL }}/g" src/environments/environment.prod.ts
          sed "s/URL_SERVER_TOKEN/${{ secrets.SERVERTOKENURL }}/g" src/environments/environment.prod.ts
          sed "s/REPORT SECTION ID MARKETING/${{ secrets.MARKETING }}/g" src/environments/environment.prod.ts
          sed "s/REPORT SECTION ID Ventas - Reporte General/${{ secrets.VENTASREPORTEGENERAL }}/g" src/environments/environment.prod.ts
          sed "s/REPORT SECTION ID Ventas - Detalle de Proyecto/${{ secrets.VENTASDETALLEPROYECTO }}/g" src/environments/environment.prod.ts

      - uses: actions/checkout@v4
      - run: npm run build

      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: '${{ secrets.GITHUB_TOKEN }}'
          firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_AMBIENSA_9C628 }}'
          channelId: live
          projectId: ambiensa-9c628